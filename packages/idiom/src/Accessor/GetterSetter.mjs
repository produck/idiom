import { GetterSetter } from './Generator.mjs';

export const errors = GetterSetter('errors');
export const length = GetterSetter('length');
export const name = GetterSetter('name');
export const cause = GetterSetter('cause');
export const message = GetterSetter('message');
