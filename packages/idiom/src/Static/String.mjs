import { String } from '../Global.mjs';
import { Getter } from '../Accessor/index.mjs';

export const fromCharCode = Getter.fromCharCode(String);
export const fromCodePoint = Getter.fromCodePoint(String);
export const raw = Getter.raw(String);
